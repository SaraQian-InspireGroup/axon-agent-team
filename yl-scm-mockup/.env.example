# 可选：生产环境 backend API 根路径（默认 /api/v1，开发时由 Vite 代理到 localhost:8000）
# VITE_API_BASE_URL=https://your-backend.example.com/api/v1

# 计划/履约 mockup 独立 API（yl-scm-mockup-api，默认 localhost:5001）
# VITE_MOCKUP_API_BASE_URL=http://localhost:5001/api/v1
